<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movie List</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <h1>Movie Watchlist App</h1>
    <ul>
    <% movies.forEach( el => { %>
            <li class='movieItem' data-id='<%=el._id%>'>
                <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.movie %></span>
<!-- it's checking to see if that specific list item so that to-do task has a completed value of true if it is it gives it a class of completed if it's not it gives it a class of not -->
                <span class='del'> Delete </span>
            </li>
    <% }) %>    
    </ul>

    <h2><%= user.userName %> has <%= left %> things left to watch.</h2>

    <form action="/movies/createMovie" method='POST'>
        <input type="text" placeholder="Enter Movie Item" name='movieItem'>
        <input type="submit">
    </form>

    <a href="/logout">Logout</a>

    <script src="js/main.js"></script>
</body>
</html>

<!-- Really all of all our views are doing is they're rendering some stuff from the database and they're sending a bunch of routes up to the server right they're sending a bunch of routes up to the server and the server is handling the thinking -->